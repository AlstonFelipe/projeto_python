"""
1. variaveis devem ter o nome descritivo
2. variaveis devem ser escritas com letras minusculas
3. nao deve possuir espaços dentro dos nomes de variaveis
4. com excessão do _ (underline) não se deve usar caracteres especiais e acentuações.
"""
# print deve ser colocado tudo em minusculo, é uma função do código que pode chamar os valores de variaveis
# e tambem instruir o usiário dentro do que ele deve fazer.



nome = "Alston"
print(nome)

# uma variavel pode ser de 3 tipos diferentes
# 1. String: é um conjunto de caracteres que são identificados por "" aspas duplas, ou '' aspas simples.
# 2. Number: é um valor numérico que não deve ter nenhuma aspas
# 3. Boolean: guarda valores verdadeiros ou falsos (sim ou não) zero ou 1, true or false
# o sinal \n contra barra e a letra n, tem a função de quebra de linha.

idade_alston = 45
if (idade_alston) >= 18:
    print ("voce pode acessar")

# para tirar uma raiz quadrada de um numero, basta vc multiplicar o numero por 0.5 exemplo: raiz quadrada de 81 fica 81**0.5 o resultado da 9


#caracteres aritmeticos: + soma, - subtração, / divisão e * multiplicação.
# % resto (serve para verificar se os numeros sao pares ou impares) e ** expoente 

# > maior, < menor,  = um sinal apenas de igual, significa que estou atribuindo a conta
# == quando utilizo o igual igual, duas vz, signfica que estou fazendo um comparativo.
# ! exclamação signfica diferença, ou != tambem serve para diferença.
# <= ou >= menor igual, ou maior igual


'''
if (condicao verdade):
    bloco de codigo identado pertence ao if

else:
    nao precisa de condição da verdade, ele vai fazer de qualquer jeito,
    cloco de codigo identado que pertence ao else

elif: (condicao verdade):
    bloco de codigo identado pertencente ao elif

    


'''

# o comando .upper () deixa todas as letras em maiuscula.
# .lower() deixa todas as letras em minusculas.
# and todas as condições tem que ser verdadeiras.
# or apenas uma condição tem que ser verdadeira.

# control d, serve para selecionar sempre o comando abaixo: caso digita errado


